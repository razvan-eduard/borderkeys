# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 BorderKeys contributors
#
# The `plus` flavour, published as its own package so that it and `core` are a choice a user can
# decline to make. With one applicationId, installing this would replace the other: the
# repository could only list one of them, and taking the assistant would mean giving up the
# settings and the learned dictionary of the build it replaced. As separate packages both can be
# listed and both can be installed, at the cost of them learning separately.
#
# No AntiFeatures. This adds llama.cpp (MIT, compiled from the vendored source) and the libc++ it
# links against (Apache-2.0 with the LLVM exception). It bundles no model and cannot fetch one:
# there is still no INTERNET permission. A model arrives only when the user picks a file whose
# SHA-256 matches an entry compiled into the build, and all three of those are Apache-2.0. See
# docs/licensing.md section 2.2.1.

Categories:
  - System
  - Writing

License: GPL-3.0-or-later
AuthorName: BorderKeys contributors
SourceCode: https://github.com/razvan-eduard/borderkeys
IssueTracker: https://github.com/razvan-eduard/borderkeys/issues
Changelog: https://github.com/razvan-eduard/borderkeys/releases

AutoName: BorderKeys +

Summary: BorderKeys with an on-device text assistant

Description: |-
    Everything the core build is — an Android keyboard that requests zero permissions and
    contains no code capable of opening a network connection — plus a text assistant that runs
    on this device.

    The assistant is a draft box above the keys. You write in it, or it opens with the text you
    selected, and nothing reaches the application until you press insert. With a model
    installed, what is in the box can be corrected, translated, re-toned, shortened, or changed
    by an instruction you write yourself, and every version is kept so you can walk back to what
    you started with.

    * No model is bundled and none can be downloaded: there is still no INTERNET permission. A
      model arrives only when you pick a GGUF file yourself, and it is refused unless its
      SHA-256 matches one of three entries compiled into the build.
    * Inference runs in a separate process that is started when you ask for something and stops
      ninety seconds after you stop asking. The model is unloaded with it.
    * Nothing is sent anywhere. The text goes to a process on this phone and the answer comes
      back to the keyboard.
    * The assistant is refused entirely in password fields and wherever an application asks for
      no personalised learning.

    Installing this does not replace the core build. They are separate applications and can be
    used side by side, which also means they keep separate settings and learn separately.

RepoType: git
Repo: https://github.com/razvan-eduard/borderkeys.git

# The same release certificate as the core package. A build signed by anything else is rejected
# rather than quietly accepted.
AllowedAPKSigningKeys: f49409d101891a6d5c7254168b083cc4d0772cda2448cc853e8e2f331b4e5321

# No historical entries on purpose. Every tag up to and including v0.2.3 built this flavour with
# the same applicationId as core, so a build of one of those tags would produce a package called
# com.borderkeys and would be rejected against this file. The first entry belongs to the first
# tag cut after the package was split.
Builds: []

AutoUpdateMode: Version
UpdateCheckMode: Tags ^v[0-9.]+$
